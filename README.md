# PDE Project
`Gifs_question_x_part_y` are folders containing animations of the solution to the PDE in their respective question as some variable is varied.

`previous` is a folder containing earlier versions of the Python code.

`PDEPROJECT.py` is the final version of the Python code used for the project.

`PDEPROJECTFINAL.pdf` details the written project.
